export type OfferCardStyles = {
  containerClassName: string;
  imageWrapperClassName: string;
  cardInfoContainerClassName: string;
  bookmarkButtonClassName: string;
}

export type ViewType = 'main' | 'favorites' | 'near';
