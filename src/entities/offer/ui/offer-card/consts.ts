import { ElementSize } from '@shared/types';

export const DEFAULT_IMAGE_SIZE_MAIN: ElementSize = {
  width: 260,
  height: 200
};

export const DEFAULT_IMAGE_SIZE_FAVORITES: ElementSize = {
  width: 150,
  height: 110
};
